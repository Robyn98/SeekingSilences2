namespace TimsFolder
{
    public class DoorController : InteractableBehaviour
    {
        public override void Interact(PlayerMove playerMove)
        {
            /*if (fec.hasFE)
            {
                anim.SetTrigger("OpenCloseDoor"); //Set the trigger "OpenClose" which is in the Animator
                //doorAudio.PlayOneShot(doorSound);
                RandomDoorAudio();
                fec.hasFEImage.gameObject.SetActive(false); //no visual rep
                //Intercom 2
                if (!firstDoorOpen)
                {
                    AS.PlayOneShot(SIR);
                    firstDoorOpen = true;
                }
            }
            else
            {
                RandomClosedDoorAudio();
            }*/
        }
    }
}